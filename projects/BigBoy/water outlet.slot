# Water outlet
def init():
    while True:
        if F_FUNCTION:
            goto(playing)

def playing():
    def _immediate():
        if not F_FUNCTION:
            goto(exit)

    while True:
        await play("Water outlet", 128, 128)

def exit():
    goto(init)
